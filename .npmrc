# npm/pnpm registry TLS on Windows (UNABLE_TO_VERIFY_LEAF_SIGNATURE).
# Prefer a proper fix: add NODE_OPTIONS=--use-system-ca to your user or system environment (Node 22+).
node-options=--use-system-ca
strict-ssl=false
